require 'spec_helper'

describe Eda::Code::Downloader do
  it 'has a version number' do
    expect(Eda::Code::Downloader::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
